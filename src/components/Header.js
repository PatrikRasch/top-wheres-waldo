import HeaderTargets from "./HeaderTargets";

const Header = () => {
  return (
    <div className="w-screen h-36 bg-blue-300 grid justify-center items-center">
      <HeaderTargets />
    </div>
  );
};

export default Header;
